// console.log(null + null)   

// console.log(1+""+1)



console.log("10",(3+2))

Which hook should manage a value that updates frequently but should not cause re-renders?  


​​Which hook is useful for creating a stable callback that does not change between renders?


What does React rely on to preserve state when reordering elements in a list?


Which hook should run code synchronously after DOM mutations but before painting?

​​What does React use internally to maintain component state across renders?

Which hook is useful for delaying expensive UI updates?


What does React.lazy() require to show a loading UI while the component loads?


What happens if you mutate React state directly without using the setter?x

Which attribute specifies that a link should not pass search engine ranking credit?

Which HTML element is used to define the title for a <details> element?